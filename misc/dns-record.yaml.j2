apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: {{ dns_name }}
  namespace: {{ namespace }}
spec:
  endpoints:
  - dnsName: {{ fqdn }}
    recordType: A
    targets:
      - {{ ip }}
